我已经继续把 `pkg/trader` 和 `pkg/condorder` 的核心源码读了一遍，现在可以给你一份更完整、也更接近真实实现状态的“逐模块重写差距清单”。

---

# 结论先说

这套 Go 代码不是简单的“交易下单 + 行情订阅”，而是一个已经拆分出完整子系统的交易网关原型，模块边界非常清晰：

- `config`：全局配置中心
- `logger`：统一日志封装
- `protocol`：数据模型 + 协议 + 枚举
- `marketfeed`：行情客户端抽象与 CTP / 天勤实现
- `websocket`：连接与消息通道
- `trader`：交易网关核心状态机 + 登录 + 查询 + 订单 + 转账 + 持久化
- `condorder`：独立条件单子系统

而 Python 侧当前更像是：

- 已经打通了行情网关主链路
- 已经做了交易/行情隔离
- 但很多“Go 侧已经成体系的基础设施能力”还没完全等价落地

---

# 逐模块重写差距清单

## 1. `pkg/config`

### Go 已实现
- 统一配置结构
- broker 列表加载
- 默认值注入
- 行情服务配置
- 条件单配置
- CTP flow path
- 全局 `Global` 配置对象

### Python 侧现状
从架构文档看，Python 侧已经能读取配置并初始化引擎，但更像“入口配置”，而不是“全局统一配置中心”。

### 差距
- 还缺一个和 Go `Config` 对齐的统一配置模型
- broker 列表与 `GetBroker()` 的统一访问接口
- `marketfeed` / `trader` / `condorder` 的配置聚合
- 默认值策略、目录默认值、配置文件优先级策略

### 下一步建议
- 先把 Python 的配置层收敛成单一 `Config` 对象
- 把 broker、CTP、行情、条件单配置统一结构化
- 让所有子系统只读配置对象，不再散落使用环境变量或局部字典

---

## 2. `pkg/logger`

### Go 已实现
- `zap` 风格结构化日志
- console/file 输出
- 统一 `Info/Warn/Error/Fatal`
- caller skip 封装

### Python 侧现状
架构文档没有体现同等级别的日志封装能力。

### 差距
- 结构化日志格式不统一
- 日志级别和输出目标可能未统一
- 缺少和 Go 同级的调用封装
- 对交易/行情/条件单三条链路的日志关联不足

### 下一步建议
- Python 侧建立统一 logger 封装
- 输出格式统一包含模块名、conn_id、order_id、symbol 等关键字段
- 对所有业务链路加一致的上下文字段

---

## 3. `pkg/protocol`

### Go 已实现
这是非常关键的一层，基本上是整个系统的“数据契约”。

包含：
- 交易方向、开平、价格类型、条件单枚举
- `Instrument`、`Order`、`Trade`、`Position`
- `User`、`Account`、`Bank`、`TransferLog`
- JSON 编解码和消息构建
- 各种枚举字符串/整数互转

### Python 侧现状
Python 架构文档提到了 `market_quote` 和交易消息，但没有看到同等完整的协议层设计。

### 差距
- 缺少统一数据模型类
- 缺少枚举映射层
- 缺少消息构建与解析的统一规则
- 缺少条件单协议类型
- 缺少交易回包/通知/推送的稳定 schema

### 下一步建议
- Python 必须补 `protocol` 层，不然后面所有业务会继续散
- 优先做：
  - 订单/成交/持仓/账户模型
  - 枚举定义
  - 通知消息构建
  - JSON schema 固化

---

## 4. `pkg/marketfeed`

### Go 已实现
这个模块已经很成熟了：

- `MarketClient` 统一接口
- `TqMarketClient` / `CtpMarketClient`
- `CtpConnection`
- 行情订阅 / 退订
- 行情缓存 `GetQuote` / `GetAllQuotes`
- 连接状态 `IsConnected`
- 登录状态 `IsLoggedIn`
- 交易日 `GetTradingDay`
- 自动登录后重订阅
- 合约符号规范化

### Python 侧已实现
你文档里明确已经有：

- 行情引擎
- 独立 EventBus
- 行情登录、订阅、退订、推送
- `market_quote`
- 登录超时兜底
- 连接级订阅集合
- 取消订阅可达底层

### 差距
- 还没有看到完整的统一 `MarketClient` 抽象
- 行情缓存能力是否对外开放还不明确
- `IsConnected` / `IsLoggedIn` / `GetTradingDay` 这类状态查询可能缺失
- 自动重连 + 自动重订阅是否完整一致还不明确
- Tq / CTP 双实现的统一封装可能还没完全对齐

### 下一步建议
- Python 行情层优先补一个统一客户端接口
- 把缓存、状态查询、重连重订阅做成稳定能力
- 对外输出固定 `quote` schema，不要再分散成不同格式

---

## 5. `pkg/websocket`

### Go 已实现
- WebSocket 连接管理
- `conn_id`
- 收发消息
- 广播与单播
- 连接生命周期管理

### Python 侧现状
文档里已经说到了：
- WebSocket Server
- `conn_id`
- 按连接回写响应和推送

### 差距
- 连接生命周期的完整细节是否一致
- 广播、回写、断开清理是否已经和 Go 同等级
- 是否有和交易/行情引擎的稳定消息边界

### 下一步建议
- 检查 Python WebSocket 层是否已经完整实现：
  - 连接注册
  - conn_id 分配
  - 单播/广播
  - 断开清理
  - 消息路由
- 这是交易网关稳定性的基础层

---

## 6. `pkg/trader`

这是差距最大的重点之一，也是最需要按子模块拆开的部分。

---

### 6.1 `trader.go` 主状态机

### Go 已实现
- 完整状态机：
  - Init
  - Connecting
  - Connected
  - Authenticating
  - Authenticated
  - LoggingIn
  - LoggedIn
  - SettlementQuerying
  - SettlementConfirming
  - Ready
  - Stopping
  - Stopped
- 连接管理
- 用户数据
- 订单映射
- 行情注入
- 查询调度器
- 重连控制
- 持久化
- 条件单管理器挂载

### Python 侧现状
文档里交易引擎已存在，但更像功能性描述，还没看到同等完整状态机。

### 差距
- 状态机不一定完整
- 连接/登录/查询/结算单闭环可能不完整
- 用户数据模型可能不够全
- 交易侧和行情侧的关系可能还没完全结构化

### 建议
- Python 交易引擎要先补“状态机骨架”
- 其次再补各个业务动作

---

### 6.2 `message.go`

### Go 已实现
支持大量 `aid`：
- `req_login`
- `change_password`
- `peek_message`
- `insert_order`
- `cancel_order`
- `req_transfer`
- `confirm_settlement`
- `qry_settlement_info`
- `req_reconnect_trade`
- `qry_transfer_serial`
- `qry_account_info`
- `qry_account_register`
- `change_trading_account_password`
- `req_start_ctp`
- `req_stop_ctp`
- 条件单相关：
  - `insert_condition_order`
  - `cancel_condition_order`
  - `pause_condition_order`
  - `resume_condition_order`
  - `qry_condition_order`
  - `qry_his_condition_order`

### Python 侧现状
架构文档里提到了交易链路，但没有看到这么完整的消息分发矩阵。

### 差距
- 消息类型覆盖度不足
- 条件单消息入口可能还没完整
- 查询类入口可能没全部补齐
- 系统消息与用户消息路由规则可能还不够细

### 建议
- Python 交易侧先做 `aid` 路由表
- 按 Go 的消息类型逐项补齐

---

### 6.3 `login.go`

### Go 已实现
- 登录请求处理
- Broker 配置读取
- 自定义 front / broker_id 覆盖
- CTP API 初始化
- 认证 -> 登录 -> 结算单 -> ready 的完整流程
- 登录超时
- 重连初始化
- 清理旧数据
- 交易日切换处理

### Python 侧现状
文档里强调了行情登录超时兜底，但交易侧登录流程是否同等级不确定。

### 差距
- 交易认证 / 登录状态机可能不完整
- 结算单流程可能没完整对齐
- 交易日切换清理机制可能缺失
- 重连机制可能不完整

### 建议
- Python 交易侧优先对齐登录流程闭环
- 结算单确认必须是完整状态流
- 交易日切换要有专门清理逻辑

---

### 6.4 `order.go`

### Go 已实现
- 下单请求解析
- 枚举兼容
- CTP 下单字段映射
- 报单引用管理
- `insertOrderSet`
- `inputOrderKeyMap`
- 下单回报
- 成交回报
- 撤单逻辑也在这里延伸

### Python 侧现状
大概率已有基本交易操作，但未必做到这么完整的 CTP 字段映射和订单 key 管理。

### 差距
- 订单状态机不一定完整
- 订单引用 / 服务器订单 key 映射可能缺失
- 下单错误回报/撤单回报闭环可能不完整
- 订单和成交的本地索引维护可能没做

### 建议
- Python 先补订单模型和订单映射管理
- 再补完整的下单/撤单/回报链路

---

### 6.5 `query.go`

### Go 已实现
- 资金账户查询
- 持仓查询
- 委托查询
- 成交查询
- 查询串行调度
- 查询完成后：
  - 初始化持仓
  - 重放成交
  - 查询银行参数
  - 查询签约关系
  - 进入 ready
  - 订阅持仓相关行情
  - 发送用户数据

### Python 侧现状
从文档看，查询链路还不是主要已完成内容。

### 差距
- 资金/持仓/委托/成交查询闭环缺失
- 查询完成后的数据初始化不足
- 持仓初始化与成交重放缺失
- 查询后通知客户端的用户数据构建缺失

### 建议
- 这是 Python 交易侧下一阶段最重要的补齐点之一
- 没有查询闭环，交易网关就不完整

---

### 6.6 `misc.go` / `transfer.go`

### Go 已实现
- 密码修改
- 资金密码修改
- 银期转账
- 银行列表/签约关系查询
- 交易通知
- 合约状态通知
- 转账回报与错误回报

### Python 侧现状
这些通常都属于交易网关的完整外围能力，文档里还没看到同级别展开。

### 差距
- 转账链路未必完整
- 密码修改未必完整
- 通知 / 错误回报未必统一
- 银行签约状态数据模型可能未做

### 建议
- 先把核心交易闭环做好
- 再补外围账户与银期能力

---

### 6.7 `persistence.go`

### Go 已实现
- 订单 key 持久化
- `LoadFromFile` / `SaveToFile`
- 按交易日区分
- 按用户区分
- 重连前保存旧数据

### Python 侧现状
文档里有提到持久化作为后续专题，但不像 Go 这么具体。

### 差距
- 订单映射持久化不足
- 交易日隔离持久化不足
- 重连/停止时保存恢复机制不足

### 建议
- Python 交易侧很快要补持久化
- 不然重连后订单映射会断

---

### 6.8 `scheduler.go`

### Go 已实现
- 查询节流
- 发送节流
- 账户/持仓查询请求计数
- 需要查询银行/签约/经纪商参数的标志位
- 保存文件标志

### Python 侧现状
文档里有“行情登录 10 秒超时兜底”，但交易查询调度这种更细粒度的节流机制未体现。

### 差距
- 查询节流调度缺失
- 发送节流缺失
- 查询状态标志不完整

### 建议
- 交易侧要补一个类似 QueryScheduler 的轻量调度器
- 尤其在查询类功能补齐之后

---

## 7. `pkg/condorder`

这是当前重写差距最大的模块之一。

### Go 已实现
条件单子系统已经比较完整了，包含：

- 条件单管理器 `Manager`
- 验证器 `Validator`
- 条件检测器 `Checker`
- 存储 `Storage`
- 条件单类型定义
- 条件单索引
- 运行/暂停/恢复/取消/查询
- 历史条件单查询
- 价格条件、时间条件、价格区间、保本止盈
- 与 `TraderCTP` 回调集成

### Python 侧现状
Python 文档只把条件单列为“后续专题”，说明这块大概率还没有做深。

### 差距
- 条件单数据模型缺失
- 条件单存储缺失
- 条件单校验缺失
- 条件触发检测缺失
- 条件单历史查询缺失
- 和 trader 的联动缺失
- 与行情价格联动缺失

### 建议
- 条件单系统应单独立项，不要和普通下单混在一起
- 需要先做：
  1. 数据模型
  2. 存储
  3. 校验
  4. 触发检测
  5. 与 trader 的执行联动

---

# 当前 Python 重写的总体差距排名

按“优先补齐程度”排序，我建议是：

## 第一优先级
1. `protocol`
2. `trader` 的状态机 + 登录 + 查询 + 用户数据闭环
3. `marketfeed` 统一客户端抽象
4. `websocket` 连接模型稳定化

## 第二优先级
5. `config`
6. `logger`
7. `persistence`
8. `query scheduler`

## 第三优先级
9. `condorder`
10. 银期转账 / 密码修改 / 签约关系查询等外围能力

---

# 建议的下一步开发顺序

如果目标是“尽快让 Python 版具备 Go 版核心能力”，我建议按这个顺序推进：

### 阶段 1
- 补齐 `protocol` 数据模型与枚举
- 统一交易/行情输出 schema
- 固化 `aid` 路由

### 阶段 2
- 完善 `trader` 主状态机
- 完成登录、结算单、查询、账户/持仓/委托/成交闭环
- 做订单 key 持久化

### 阶段 3
- 统一 `marketfeed` 客户端层
- 补缓存、状态查询、重连重订阅

### 阶段 4
- 接入 `condorder`
- 完成条件单存储、校验、触发、执行链路

---

# 一句话总结

Go 侧已经不是“功能点集合”，而是一套完整网关系统；Python 侧目前最接近的是行情链路，真正的差距主要集中在：

- 协议层
- 交易状态机
- 查询闭环
- 持久化
- 条件单子系统

如果你愿意，我下一步可以直接把这份内容整理成一份更实用的“开发任务清单”，按模块拆成可执行条目，比如：

- `protocol` 要补哪些类
- `trader` 要补哪些方法
- `condorder` 要补哪些文件
- 每一步的验收标准是什么

这样你就可以直接拿去排期。